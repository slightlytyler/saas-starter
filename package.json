{
  "name": "saas-starter",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna init && lerna exec -- linklocal && lerna bootstrap --npm-client=yarn --mutex=network",
    "configure": "echo 'I have not build configuration into the project yet, soz >:('",
    "eject": "echo 'I have not built ejection into the project yet, soz >:('",
    "precommit": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5",
    "linklocal": "^2.8.0",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "precommit"
  ]
}
